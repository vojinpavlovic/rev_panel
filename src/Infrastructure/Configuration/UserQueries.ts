export default {
    findById: "SELECT accounts, `group`, inventory, job, job_grade, firstname, lastname, sex, height, phone_number, status, vreme, health, pancir, credits FROM users WHERE identifier = ?"
}